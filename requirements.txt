# --- 基础工具库 ---
absl-py==2.3.1
antlr4-python3-runtime==4.13.2
attrs==25.4.0
click==8.3.1
immutabledict==4.2.2
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
langdetect==1.0.9
latex2sympy2_extended==1.10.2
mpmath==1.3.0
nltk==3.9.2
python-dateutil==2.9.0.post0
referencing==0.37.0
regex==2026.1.15
rfc3339-validator==0.1.4
rpds-py==0.30.0
six==1.17.0
sympy==1.14.0
tqdm==4.67.1
spacy

# --- 核心计算与 NeMo 专用验证库 ---
numpy==2.2.6
pandas==2.2.3
math-verify==0.8.0
openapi-schema-validator==0.6.3
datasets
git+https://github.com/abukharin-nv/verifiable-instructions.git
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl


# =================================================================
# ⚠️ 特殊库安装指南: verifiable-instructions
# =================================================================
# 该库是 Instruction Following (IF) 模块的核心依赖。
# 由于服务器连接 GitHub 经常出现 TLS/网络超时错误，直接运行下方 git+https 可能会失败。
#
# 【如果 pip install 失败，请执行以下手动修复步骤】：
# 1. 手动克隆代码 (如果服务器网不好，请在本地电脑下载 ZIP 后上传并解压):
#    git clone https://github.com/abukharin-nv/verifiable-instructions.git
#
# 2. 进入目录并安装:
#    cd verifiable-instructions
#    pip install -e .
#
# 3. 安装成功后，即可忽略下方这一行。
# =================================================================
# git+https://github.com/abukharin-nv/verifiable-instructions.git